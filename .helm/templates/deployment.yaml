apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "app.name" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "app.labels" $ | nindent 4 }}
    {{- include "app.datadogLabels" $ | nindent 4 }}
  {{- with .Values.additionalAnnotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  replicas: {{ .Values.replicaCount }}
  strategy:
    {{- toYaml .Values.strategy | nindent 4 }}
  selector:
    matchLabels:
      {{- include "app.selectorLabels" $ | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "app.labels" $ | nindent 8 }}
        {{- include "app.datadogLabels" $ | nindent 8 }}
      annotations:
        # Trigger a rollout when the rendered application.yaml changes.
        checksum/config: {{ include (print $.Template.BasePath "/configmap.yaml") . | sha256sum }}
        {{- with .Values.additionalAnnotations }}
        {{- toYaml . | nindent 8 }}
        {{- end }}
    spec:
      {{- with .Values.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      serviceAccountName: {{ include "app.serviceAccountName" . }}
      {{- with .Values.podSecurityContext }}
      securityContext:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      containers:
        - name: {{ include "app.name" . }}
          {{- with .Values.containerSecurityContext }}
          securityContext:
            {{- toYaml . | nindent 12 }}
          {{- end }}
          image: "{{ include "app.image" . }}"
          imagePullPolicy: "{{ .Values.image.pullPolicy }}"
          ports:
            - name: http
              containerPort: {{ .Values.settings.server.port }}
              protocol: TCP
          env:
            - name: ENVIRONMENT
              value: {{ .Values.environment | quote }}
            - name: APPLICATION_VERSION
              value: "{{ include "app.version" . }}"
            - name: ARCANE_INGESTION_CONFIG
              value: /etc/arcane-push-stream/application.yaml
            {{- if .Values.datadog.unifiedServiceTags.enabled }}
            # Mirror the Unified Service Tagging pod labels into the process environment so
            # DogStatsD/log output is attributed to the same service in DataDog.
            - name: DD_ENV
              valueFrom:
                fieldRef:
                  fieldPath: metadata.labels['tags.datadoghq.com/env']
            - name: DD_SERVICE
              valueFrom:
                fieldRef:
                  fieldPath: metadata.labels['tags.datadoghq.com/service']
            - name: DD_VERSION
              valueFrom:
                fieldRef:
                  fieldPath: metadata.labels['tags.datadoghq.com/version']
            # Enables DogStatsD origin detection so the agent can resolve pod-level tags.
            - name: DD_ENTITY_ID
              valueFrom:
                fieldRef:
                  fieldPath: metadata.uid
            {{- end }}
            {{- if .Values.resourceNamespace }}
            - name: RESOURCE_NAMESPACE
              value: {{ .Values.resourceNamespace | quote }}
            {{- end }}
            {{- with .Values.extraEnv }}
            {{- toYaml . | nindent 12 }}
            {{- end }}
          {{- with .Values.extraEnvFrom }}
          envFrom:
            {{- toYaml . | nindent 12 }}
          {{- end }}
          livenessProbe:
            httpGet:
              path: {{ .Values.settings.probes.liveness.path }}
              port: http
            initialDelaySeconds: {{ .Values.settings.probes.liveness.initialDelaySeconds }}
            periodSeconds: {{ .Values.settings.probes.liveness.periodSeconds }}
            timeoutSeconds: {{ .Values.settings.probes.liveness.timeoutSeconds }}
            successThreshold: {{ .Values.settings.probes.liveness.successThreshold }}
            failureThreshold: {{ .Values.settings.probes.liveness.failureThreshold }}
          readinessProbe:
            httpGet:
              path: {{ .Values.settings.probes.readiness.path }}
              port: http
            initialDelaySeconds: {{ .Values.settings.probes.readiness.initialDelaySeconds }}
            periodSeconds: {{ .Values.settings.probes.readiness.periodSeconds }}
            timeoutSeconds: {{ .Values.settings.probes.readiness.timeoutSeconds }}
            successThreshold: {{ .Values.settings.probes.readiness.successThreshold }}
            failureThreshold: {{ .Values.settings.probes.readiness.failureThreshold }}
          {{- if .Values.settings.probes.startup.enabled }}
          startupProbe:
            httpGet:
              path: {{ .Values.settings.probes.startup.path }}
              port: http
            periodSeconds: {{ .Values.settings.probes.startup.periodSeconds }}
            failureThreshold: {{ .Values.settings.probes.startup.failureThreshold }}
          {{- end }}
          {{- with .Values.resources }}
          resources:
            {{- toYaml . | nindent 12 }}
          {{- end }}
          volumeMounts:
            - name: config
              mountPath: /etc/arcane-push-stream
              readOnly: true
            - name: tmp
              mountPath: /tmp
            {{- with .Values.extraVolumeMounts }}
            {{- toYaml . | nindent 12 }}
            {{- end }}
      volumes:
        - name: config
          configMap:
            name: {{ include "app.configMapName" . }}
        - name: tmp
          emptyDir: {}
        {{- with .Values.extraVolumes }}
        {{- toYaml . | nindent 8 }}
        {{- end }}
      {{- with .Values.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
